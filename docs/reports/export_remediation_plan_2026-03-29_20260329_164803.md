# Export Remediation Plan (2026-03-29 20260329_164803)

## Prioritized Actions (Top 30 backlog source)
Source files:
- `outputs/session_runs/logs/20260329_164803_remaining_export_gaps_top30.csv`
- `outputs/session_runs/logs/20260329_164803_remaining_export_gaps_top30.json`

## Grouped by Script Ownership

### 1) Export Pipeline Core (`export_building_fbx.py`, `bake_utils.py`)
- Batch-generate GLB sidecars for legacy FBX-only exports.
- Keep FBX + GLB dual outputs as default contract.
- Add optional fast mode (`--texture-size 1024`) for overnight smoke throughput.

### 2) Validation (`validate_export_pipeline.py`)
- Current: GLB fallback + scene coercion + auxiliary-file filtering is in place.
- Next:
  - Add explicit open-shell classification mode (architectural meshes) so non-watertight is surfaced as category, not hard blocker for readiness tiers.
  - Add optional stricter profile for game collision-focused QA.

### 3) Collision/LOD (`generate_collision_mesh.py`, `generate_lods.py`)
- Apply Blender-5-compatible fixes already merged in this run to broader batch execution.
- Batch-generate `_collision` and `_LOD0..3` for priority addresses first (top 30 queue, then remainder).
- Add naming normalization hook to always emit underscore-safe filenames matching validator expectations.

### 4) Engine Manifests (`build_unity_prefab_manifest.py`, `build_unreal_datasmith.py`)
- Recursive scanning fix is in place.
- Next:
  - Add filters to restrict manifests to an input address list / smoke subset.
  - Add manifest schema checks in CI for required fields and file existence.

## Overnight Automation Sequence (recommended)
1. Export+GLB for target list:
   - `blender --background outputs/full_v2/<addr>.blend --python scripts/export_building_fbx.py -- --address <addr> --texture-size 1024`
2. Generate collision:
   - `blender --background --python scripts/generate_collision_mesh.py -- --blend outputs/full_v2/<addr>.blend --output-dir outputs/exports/<addr>`
3. Generate LODs:
   - `blender --background --python scripts/generate_lods.py -- --blend outputs/full_v2/<addr>.blend --output-dir outputs/exports/<addr>`
4. Validate:
   - `python scripts/validate_export_pipeline.py --exports-dir outputs/exports --address <addr>`
5. Rebuild manifests:
   - `python scripts/build_unity_prefab_manifest.py --exports-dir outputs/exports --output outputs/exports/unity_prefab_manifest_<stamp>.json`
   - `python scripts/build_unreal_datasmith.py --exports-dir outputs/exports --output outputs/exports/unreal_datasmith_<stamp>.udatasmith`

## Suggested Next 24h Queue
1. Run GLB-sidecar backfill for all root-validation FAIL addresses caused by FBX loader limits.
2. Run collision+LOD generation on the top 30 gap list first.
3. Re-run root validation and compute FAIL reduction vs `20260329_164803_validation_report_root.json`.
4. Promote a “known-good export set” gate based on:
   - no load errors
   - no degenerate faces
   - acceptable watertight profile according to selected QA mode.
